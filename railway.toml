[build]
builder = "NIXPACKS"
buildCommand = "NODE_ENV=production NEXT_TELEMETRY_DISABLED=1 npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

